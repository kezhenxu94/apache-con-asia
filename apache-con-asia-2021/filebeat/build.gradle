plugins {
    id 'com.avast.gradle.docker-compose' version '0.7.1'
}

repositories {
    mavenCentral()
}

dependencies {
}

dockerCompose {
    useComposeFiles = ['docker-compose.kafka.yml', 'docker-compose.yml']
}
